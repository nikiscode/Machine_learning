## Machine_learning
This module includes the models that trains the or used to train the machine 
there are 3 types in this 
1.Supervised learning:
Traning the model with the labeled data
2.Unsupervised learning :
 Traning the model with the unlabeled data
3.Reinforcement learning:
Learning from the mistakes and credits are given for the good work and some negative sentences are offerd for wrong wrok as we cannot punis the model the only of traning the model is by giving a best explanationa and proper gudience to the system by the proper trained data

